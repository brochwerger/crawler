version: '3.7'

services:

  c1:
    image: nginx

    networks:
      - test

    volumes:
      - ${TESTS_DIR}/c1.html:/usr/share/nginx/html/index.html

#    ports:
#      - "80:80"

  c2:
    image: nginx

    networks:
      - test

    volumes:
      - ${TESTS_DIR}/c2.html:/usr/share/nginx/html/index.html

  c3:
    image: nginx

    networks:
      - test

    volumes:
      - ${TESTS_DIR}/c3.html:/usr/share/nginx/html/index.html

networks:
  test:
    driver: overlay
    attachable: true